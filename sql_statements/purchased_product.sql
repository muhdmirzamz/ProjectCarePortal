CREATE TABLE `Application`.`PurchasedProductDetail` (
  `productName` VARCHAR(45) NOT NULL COMMENT '',
  `category` VARCHAR(45) NOT NULL COMMENT '',
  `quantity` INT NOT NULL COMMENT '',
  `productUnitPrice` DOUBLE NOT NULL COMMENT '');
